spring.application.name=mylifediary

# JWT settings (prefer environment variables in real deployments)
jwt.secret=${JWT_SECRET:change-this-secret-in-production-minimum-32-bytes}
jwt.expiration=${JWT_EXPIRATION:3600}

